
                var entidad = new BandejaEnvioSmsModel
                {
                    NumeroTarjeta = tarjeta.NumeroTarjeta,
                    Sms = request.ContenidoSMS.Replace("{PIN}", pin),
                    Celular = request.Telefono,
                    Telco = request.OperadorTelefono,
                    Referencia = request.Telefono,
                    FechaInicio = DateTime.Now,
                    TipoTarjeta = tipoTarjeta,
                    Estado = 0,
                    DescripcionEstado = "Mensaje No Enviado"
                };




Tengo una consulta en esta linea   Sms = request.ContenidoSMS.Replace("{PIN}", pin), y es que, ya sabemos que no podemos guardar ese pin ... pero como lo manjeamos ? en memoria? en alguna tabla a nivel de codigo? porque los correos en la tabla bandeja van a quedar como en cola, o no? 
