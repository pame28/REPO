if (_logger.IsEnabled(LogLevel.Information))
{
    var ultimos4 = request.CounterpartCard?.Length >= 4
        ? request.CounterpartCard[^4..]
        : request.CounterpartCard;

    _logger.LogInformation(
        "Request recibido. CounterpartCard: {Ultimos4}",
        ultimos4);
}