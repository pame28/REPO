using System.Data.Odbc;
using SwapAuthApi.Data.Interfaces;
using SwapAuthApi.Models;

namespace SwapAuthApi.Data
{
    public class ApiTokenRepository : IApiTokenRepository
    {
        private readonly IConfiguration _configuration;

        public ApiTokenRepository(IConfiguration configuration)
        {
            _configuration = configuration;
        }

        public async Task<ApiToken> GetTokenAsync()
        {
            var connectionString = _configuration.GetConnectionString("AS400");

            using var connection = new OdbcConnection(connectionString);
            await connection.OpenAsync();

            string query = @"
                SELECT 
                    APICLIENID AS CLIENTID,
                    APISECREID AS SECRETID
                FROM TCATV3DAT.TCACCTOK
                WHERE APISERVICE = 'SERVWEB'
                FETCH FIRST 1 ROW ONLY";

            using var command = new OdbcCommand(query, connection);

            using var reader = await command.ExecuteReaderAsync();

            if (await reader.ReadAsync())
            {
                return new ApiToken
                {
                    ClientId = reader["CLIENTID"].ToString()!.Trim(),
                    SecretId = reader["SECRETID"].ToString()!.Trim()
                };
            }

            throw new Exception("Credenciales no encontradas en AS400");
        }
    }
}

















using System.Text;
using SwapAuthApi.Data.Interfaces;
using SwapAuthApi.Services.Interfaces;
using SwapAuthApi.Utilities;

namespace SwapAuthApi.Services
{
    public class TokenService : ITokenService
    {
        private readonly IApiTokenRepository _repository;
        private readonly IConfiguration _configuration;

        public TokenService(IApiTokenRepository repository, IConfiguration configuration)
        {
            _repository = repository;
            _configuration = configuration;
        }

        public async Task<string> GenerateAuthorizationAsync()
        {
            var token = await _repository.GetTokenAsync();

            // clientId:secretId
            string credentials = $"{token.ClientId}:{token.SecretId}";

            // Base64
            string base64Credentials = Convert.ToBase64String(
                Encoding.UTF8.GetBytes(credentials)
            );

            // secretKey compartido
            string secretKey = _configuration["Security:SecretKey"]!;

            // AES256
            string encrypted = AES256Utils.Encrypt(base64Credentials, secretKey);

            return $"Basic {encrypted}";
        }
    }
}













using Microsoft.AspNetCore.Mvc;
using SwapAuthApi.Services.Interfaces;

namespace SwapAuthApi.Controllers
{
    [ApiController]
    [Route("api/auth")]
    public class AuthController : ControllerBase
    {
        private readonly ITokenService _tokenService;

        public AuthController(ITokenService tokenService)
        {
            _tokenService = tokenService;
        }

        [HttpGet("token")]
        public async Task<IActionResult> GetToken()
        {
            var token = await _tokenService.GenerateAuthorizationAsync();

            return Ok(new
            {
                Authorization = token
            });
        }
    }
}
