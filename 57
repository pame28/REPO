Mira, no se si esto nos ayude a tener claridad


Anexo:
Clase usada para encriptado de información, los métodos son estáticos para poder llamarlos 
desde cualquier punto, únicamente enviando el texto más el secretKey definido y compartido.
public class AES256Utils {
private static final String ALGORITHM = "AES/GCM/NoPadding";
 private static final int TAG_LENGTH_BIT = 128;
 private static final int IV_SIZE_BYTE = 12;
 private static final String KEY_ALGORITHM = "AES";
 private AES256Utils() {}
 public static String encrypt(String plainText, String secretKey) {
 try {
 byte[] iv = generateIV();
 SecretKeySpec keySpec = buildKey(secretKey);
 Cipher cipher = Cipher.getInstance(ALGORITHM);
 GCMParameterSpec spec = new GCMParameterSpec(TAG_LENGTH_BIT, iv);
 cipher.init(Cipher.ENCRYPT_MODE, keySpec, spec);
 byte[] cipherText = 
cipher.doFinal(plainText.getBytes(StandardCharsets.UTF_8));
 byte[] combined = ByteBuffer.allocate(iv.length + cipherText.length)
 .put(iv)
11
 .put(cipherText)
 .array();
 return Base64.getEncoder().encodeToString(combined);
 } catch (Exception e) {
 throw new RuntimeException("Error encriptando: " + e.getMessage(), e);
 }
 }
 public static String decrypt(String cipherTextBase64, String secretKey) {
 try {
 byte[] combined = Base64.getDecoder().decode(cipherTextBase64);
 byte[] iv = new byte[IV_SIZE_BYTE];
 System.arraycopy(combined, 0, iv, 0, iv.length);
 byte[] encryptedText = new byte[combined.length - IV_SIZE_BYTE];
 System.arraycopy(combined, IV_SIZE_BYTE, encryptedText, 0, 
encryptedText.length);
 SecretKeySpec keySpec = buildKey(secretKey);
 Cipher cipher = Cipher.getInstance(ALGORITHM);
 GCMParameterSpec spec = new GCMParameterSpec(TAG_LENGTH_BIT, iv);
 cipher.init(Cipher.DECRYPT_MODE, keySpec, spec);
 byte[] decryptedText = cipher.doFinal(encryptedText);
 return new String(decryptedText, StandardCharsets.UTF_8);
 } catch (Exception e) {
 throw new RuntimeException("Error desencriptando: Datos corruptos o clave 
inválida", e);
 }
 }
 private static SecretKeySpec buildKey(String secret) throws Exception {
 MessageDigest sha = MessageDigest.getInstance("SHA-256");
 byte[] key = sha.digest(secret.getBytes(StandardCharsets.UTF_8));
 return new SecretKeySpec(key, KEY_ALGORITHM);
 }
 private static byte[] generateIV() {
 byte[] iv = new byte[IV_SIZE_BYTE];
 new SecureRandom().nextBytes(iv);
 return iv;
 }
