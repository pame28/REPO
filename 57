string mensajeLog = mensajeFinal.Replace(pin, "****");

_logger.LogDebug(
    "BiMovil Request -> Telefono:{Telefono} Operador:{Operador} Mensaje:{Mensaje} Referencia:{Referencia}",
    telefonoFinal,
    operadorFinal,
    mensajeLog,
    referencia
);